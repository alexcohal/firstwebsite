function scrollToProjects() {
  event.preventDefault();
    const element = document.getElementById('below_welcome');
    element.scrollIntoView({ behavior: 'smooth' });
  }
  function scrollToCapacities() {
    event.preventDefault();
    const element = document.getElementById('cap');
    element.scrollIntoView({ behavior: 'smooth' });
  }
  function scrollToContact() {
    event.preventDefault();
    const element = document.getElementById('ch2');
    element.scrollIntoView({ behavior: 'smooth' });
  }



  
